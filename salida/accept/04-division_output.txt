#include <stdlib.h>

#include <stdio.h>

typedef enum {
    ORDER_ASCENDING,
    ORDER_DESCENDING
} Order;

int compareAsc(const void *a, const void *b) {
    int intA = *(const int*)a;
    int intB = *(const int*)b;
    return (intA > intB) - (intA < intB);
}

int compareDesc(const void *a, const void *b) {
    int intA = *(const int*)a;
    int intB = *(const int*)b;
    return (intB > intA) - (intB < intA);
}

void sortArray(int *array, int length, Order order) {
    if (array == NULL || length <= 0) return;
    if (order == ORDER_ASCENDING) {
        qsort(array, length, sizeof(int), compareAsc);
    } else {
        qsort(array, length, sizeof(int), compareDesc);
    }
}

if (3 < 10) {
sortArray(arr_intTutti, sizeof(arr_intTutti) / sizeof(arr_intTutti[0]), ORDER_ASCENDING);
}
func_bool()void foo() {
while (intvar <= intnar) {
intnar++;
printf("%s\n", stringJUAN);
if (intnar > intvar) {
printf("1\n", inttito);
}
printf("%s\n", "Goodbye");
printf("%s\n", "Hello");
}
if (boolA) {
printf("%s\n", "A");
} else if ((intla == 0)) {
printf("%s\n", "a");
} else {
printf("%s\n", "brbr patapim");
}

printf("%s\n", "FOO");
}

